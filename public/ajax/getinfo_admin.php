<?php
//session_start();
//if (($_SESSION['auth'] == true) AND ($_SESSION['admin'] == 1)){
//    require_once ( '../db.connect.php');

    /*if (isset($_POST['id_admin'])) { //проверяем, есть ли переменная
        //удаляем строку из таблицы
        $id = $_POST['id_admin'];
        if ($_POST['value'] == 'on'){
            $value = 1;
        } else $value = 0;

        // $id =13;
        $sql = "UPDATE users SET admin = :userAdmin WHERE id_user = :userID";
        $stmt = $db->prepare($sql);
        $stmt->bindParam(':userID', $id, PDO::PARAM_INT);
        $stmt->bindParam(':userAdmin', $value, PDO::PARAM_INT);
        $stmt->execute();
    }
}*/
